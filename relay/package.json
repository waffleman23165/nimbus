{
  "name": "nimbus-relay",
  "private": true,
  "version": "0.1.0",
  "description": "Partner-flowing relay for Nimbus: Cloudflare Worker + one Durable Object per room.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.137.0"
  }
}
